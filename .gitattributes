# Normalize line endings: LF in the repo, checked out native on each OS.
* text=auto eol=lf

# Windows launcher must stay CRLF for cmd.exe.
*.bat text eol=crlf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.icns binary
